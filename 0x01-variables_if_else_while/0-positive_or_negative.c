#include<stdlib.h>
#include<time.h>
/**
 * main- Entry point
 *
 * Return: 0(success)
 *
 */

int main(void)
{
	int n;
	srand(time(0)\n);
	n = rand() - RAND_MAX / 2;
	printf("Enter a number: ");
	scanf("/%d ,n);

	if(n > 0)
{
	printf("is negative\n");
	else if(n < 0)
{
	printf("is positive\n");
	else(n = 0)
{
	printf("is zero\n");

	return (0);
}

